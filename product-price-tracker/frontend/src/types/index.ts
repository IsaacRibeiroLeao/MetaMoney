export interface Product {
  id?: number;
  name: string;
  price: number;
  locked?: boolean;
  timestamp?: string;
}

export interface Venda {
  id?: number;
  name: string;
  price: number;
  locked?: boolean;
  timestamp?: string;
}
